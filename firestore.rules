rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Production Security: Deny all direct client-side unauthenticated read/writes.
      // Server-side administrative API and authenticated sessions are used for secure access.
      allow read, write: if request.auth != null;
    }
  }
}
